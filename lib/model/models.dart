class ModelTarefas {
  String tarefa;
  String descricao;

  DateTime datafinal;
  bool estacompleta;

  ModelTarefas(
      {this.tarefa, this.descricao, this.datafinal, this.estacompleta});
}
